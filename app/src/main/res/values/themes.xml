<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CipherChat" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <!-- Color Primary: It is the color displayed most frequently across your app’s screens and components.-->
        <item name="colorPrimary">@color/app_primary_color</item>
        <!-- Color PrimaryVariant: It is used to distinguish two elements of the app using the primary color, such as the top app bar and the system bar.-->
        <item name="colorPrimaryVariant">@color/white</item>
        <!-- ColorOnPrimary: Color used for text and icons displayed on top of the primary color.-->
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary brand color. -->
        <!-- Color Secondary: The secondary color provides more ways to accent and distinguish your product.-->
        <item name="colorSecondary">@color/teal_200</item>
        <!-- Color SecondaryVariant:The secondary variant color is used to distinguish two elements of the app using the secondary color.-->
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <!-- ColorOnSecondary: Color used for text and icons displayed on top of the secondary color.-->
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Theme for splash screen-->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="s">
            @android:color/transparent
        </item>
        <item name="postSplashScreenTheme">@style/Theme.CipherChat</item>
    </style>

    <!--    Style for the button-->
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="materialThemeOverlay">@style/ButtonColor</item>
        <item name="android:textAppearance">@style/TextAppearance.App.Button</item>
        <item name="shapeAppearance">@style/ShapeAppearance.App.SmallComponent</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ButtonColor">
        <item name="android:backgroundTint">@color/app_primary_color</item>
    </style>

    <style name="TextAppearance.App.Button" parent="TextAppearance.MaterialComponents.Button">
        <item name="fontFamily">@font/roboto_medium</item>
        <item name="android:fontFamily">@font/roboto_medium</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="ShapeAppearance.App.SmallComponent" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!--    Style for Bottom Navigation-->
    <style name="BottomNavigationBar">
        <item name="backgroundTint">@color/white</item>
        <item name="itemIconTint">@drawable/bottom_nav_view_item_selector</item>
        <item name="itemTextColor">@drawable/bottom_nav_view_item_selector</item>
    </style>

    <style name="textviewStyle">
        <item name="android:textColor">@color/black</item>
    </style>

    <!--    Icon colors-->
    <style name="iconColor">
        <item name="tint">@color/black</item>
    </style>

    <style name="toolbarTint">
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="cardViewStyle">
        <item name="cardBackgroundColor">#F3F6F6</item>
    </style>
</resources>